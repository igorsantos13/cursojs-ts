function greet(name){
  return `Olá, ${name}`;
}

console.log(greet('Lewis'));

const userName = 'Peter'
export default userName

//para compilar código no terminal execute: npx tsc "nome do arquivo"
//ao fazer isso se inicia o processo de compilação para a criação de um arquivo .js
//o objetivo de ser executado pelo node


//fun fact: caso seu código ts esteja escrito em js (o que obviamente nao é o recomendado)
//o node consegue rodar o código sem problemas
